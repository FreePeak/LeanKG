# REL-076: MCP Wave 1b hard-delete evidence

**Date:** 2026-08-01  
**Tracker:** `US-SURF-08..11` / `FR-SURF-12..15` / `REL-076`  
**PRD:** §3.16 / §5.18 (`v3.8.3-mcp-surf-1b`)

## Summary

Hard-deleted remaining true-subset MCP tools:

| Removed | Keep instead |
|---------|--------------|
| `load_layer` | `get_overview_context` (L0/L1), `get_cluster_context` (L2), `search_code` / `find_function` (L3) |
| `get_doc_structure` | `get_doc_tree` |

Closes FR-SURF-06 (doc-structure unify) as **delete-weaker**, not format-flag merge.

## `tools/list` delta

| Surface | Approx count | `load_layer` | `get_doc_structure` |
|---------|-------------:|--------------|---------------------|
| Pre–Wave 1b (`tools.rs` string inventory) | 89 | present | present |
| Post–Wave 1b | 87 | **absent** | **absent** |

Delta: **−2** registered tools (string inventory). Runtime without `--features embeddings` is 2 lower (`embed_control` + `kg_semantic_context` cfg-gated).

## Hard-removed set (cumulative)

`mcp_hello`, `mcp_impact`, `get_doc_for_file`, `find_clones`, `wake_up`, `search_by_environment`, **`load_layer`**, **`get_doc_structure`**.

## Prefer-order (canonical post–Wave 1b)

| Chain | Tools |
|-------|-------|
| Overview | `get_overview_context` → optional `get_architecture` |
| Search | `concept_search` → `semantic_search` → `search_code` |
| Env | `env=` on search / `kg_*` |
| File context | `get_context` |
| Doc structure | `get_doc_tree` only |

## Keep-both (not deleted)

Search/semantic prefer-order triples; `get_context`↔`ctx_read`; `orchestrate`↔`query_graph`; callers↔call_graph; cluster trio; Android nav; `get_god_nodes`↔`get_graph_report`; PRD/traceability tools.

## Tests

```bash
cargo test --release --test redundant_tools_matrix
cargo test --release --test mcp_tools_redundancy_tests
cargo test --release --test mcp_tools_full_tests
```

## Synced surfaces

- `src/mcp/tools.rs`, `src/mcp/handler.rs`
- `tests/redundant_tools_matrix.rs` (+ classified previously missing ontology/PRD/export tools)
- Smoke / install / CLAUDE / GEMINI / mcp-tools / agentic-instructions / using-leankg skill
